export enum UsersMicroserviceMessages {
  USERS_MS_PING = `USERS_MS_PING`,

  CREATE_USER = `CREATE_USER`,
  UPDATE_USER = `UPDATE_USER`,
}

export enum UsersMicroserviceEvents {
  USERS_MS_PINGED = `USERS_MS_PINGED`,

  USER_CREATED = `USER_CREATED`,
  USER_UPDATED = `USER_UPDATED`,
}